# Геймтон DatsJingleBang - Клиент для игры

Интеллектуальный клиент для игры DatsJingleBang с оптимизированной стратегией победы.

## 🚀 Быстрый старт

### Запуск (один скрипт запускает всё!)

1. **Настройте .env:**
   ```bash
   cp .env.example .env
   # Отредактируйте .env и добавьте API_KEY
   ```

2. **Запустите всё одной командой:**
   ```bash
   python web_server.py
   ```

   Это запустит:
   - ✅ Веб-сервер с визуализатором
   - ✅ Игровой клиент (автоматически)
   - ✅ Кэширование данных
   - ✅ Анализ карты

3. **Откройте визуализатор:**
   - Перейдите на http://localhost:5000/
   - Карта автоматически центрируется на бомберах
   - Видны все 6 бомберов с номерами (1-6)

### Альтернатива: Только клиент (без визуализации)

```bash
python game_client.py
```

## 📊 Визуализация

### С локальным сервером:
1. Запустите `python web_server.py`
2. Откройте http://localhost:5000/ в браузере
3. Включите чекбокс "Использовать локальный сервер"

### Без локального сервера:
1. Откройте `visualizer.html` в браузере
2. Введите API ключ и URL сервера

## 🧠 Анализ карты и улучшение стратегии

Клиент автоматически анализирует карту и улучшает стратегию:

- **Кластеры препятствий**: Находит группы препятствий для максимизации очков
- **Высокоценные цели**: Приоритизирует цели, где одна бомба уничтожит несколько препятствий
- **Безопасные зоны**: Определяет безопасные области для отступления
- **Плотность препятствий**: Адаптирует стратегию в зависимости от плотности

## 📁 Структура проекта

- `game_client.py` - основной клиент игры
- `game_api.py` - общий модуль для работы с API (кэширование, анализ)
- `web_server.py` - веб-сервер для совместного использования данных
- `visualizer.html` - визуализатор игры
- `openapi.json` - спецификация API

## ⚙️ Настройка

Создайте файл `.env`:
```
API_KEY=your_api_key_here
BASE_URL=https://games-test.datsteam.dev
```

## 🎯 Стратегия победы

Клиент реализует комплексную стратегию:

1. **Приоритет на уничтожение препятствий группами** - максимизация очков (1+2+3+4 = 10)
2. **Безопасность превыше всего** - автоматическое отступление от опасностей
3. **Умное планирование пути** - алгоритм A* с учетом анализа карты
4. **Оптимизированная система улучшений** - приоритет: радиус → скорость → бомбы
5. **Анализ карты в реальном времени** - адаптация стратегии под текущую карту

## 🔧 Команды

```bash
# Клиент с локальным сервером
python game_client.py --local

# Клиент с подробным выводом
python game_client.py --local --verbose

# Прямое подключение
python game_client.py

# Веб-сервер
python web_server.py
```

## 📈 Статистика

Визуализатор показывает полную статистику:
- Счёт и пройденный путь
- Очки за уничтожение (препятствия/юниты/мобы)
- Количество бомб и уничтожений
- Возрождения и штрафы

## ⚠️ Ограничения

- Максимум 1 запрос в секунду к API (для избежания ошибок 429)
- Кэширование данных на 350мс
- Автоматическое увеличение интервалов при ошибках 429
