# Настройка и использование

## 🚀 Рекомендуемый способ (с локальным сервером)

### Преимущества:
- ✅ Один запрос к серверу для клиента и визуализатора
- ✅ Кэширование данных (меньше нагрузка)
- ✅ Анализ карты в реальном времени
- ✅ Улучшенная стратегия

### Шаги:

1. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

2. **Настройте .env:**
   ```bash
   cp .env.example .env
   # Отредактируйте .env и добавьте API_KEY
   ```

3. **Запустите веб-сервер** (в первом терминале):
   ```bash
   python web_server.py
   ```
   
   Вы увидите:
   ```
   🚀 Запуск веб-сервера на http://localhost:5000
   📊 Визуализатор: http://localhost:5000/
   🔌 API: http://localhost:5000/api/arena
   📈 Анализ карты: http://localhost:5000/api/map-analysis
   ```

4. **Запустите клиент** (во втором терминале):
   ```bash
   python game_client.py --local
   ```

5. **Откройте визуализатор:**
   - Перейдите на http://localhost:5000/
   - Или откройте `visualizer.html` и включите "Использовать локальный сервер"

## 📊 Как это работает

```
┌─────────────┐
│  Игровой    │
│  сервер     │
└──────┬──────┘
       │
       │ (1 запрос/сек)
       │
┌──────▼──────────┐
│  Локальный      │
│  веб-сервер     │◄─── Кэширование
│  (web_server)   │     Анализ карты
└───┬─────────┬───┘
    │         │
    │         │
┌───▼───┐ ┌──▼──────────┐
│Клиент │ │Визуализатор │
│       │ │             │
└───────┘ └─────────────┘
```

## 🔍 Анализ карты

Веб-сервер автоматически анализирует карту:
- Находит кластеры препятствий
- Определяет высокоценные цели
- Выявляет безопасные зоны
- Рассчитывает плотность препятствий

Клиент использует этот анализ для улучшения стратегии!

## ⚙️ Альтернативный способ (без локального сервера)

Если не хотите использовать локальный сервер:

```bash
# Клиент
python game_client.py

# Визуализатор - откройте visualizer.html и введите API ключ
```

## 🐛 Решение проблем

### Ошибка "API_KEY не найден"
- Создайте файл `.env` с `API_KEY=your_key`

### Ошибка "Connection refused" при использовании --local
- Убедитесь, что `web_server.py` запущен

### Ошибки 429
- Веб-сервер автоматически кэширует данные
- Используйте локальный сервер для уменьшения запросов

